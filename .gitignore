tradex/archive
.DS_Store
*/.DS_Store
*.log
*.tmp
*.target/

# PHP config files with sensitive values (upload manually to server, never commit)
thelaminars/config.php
*/config.php

# MathTrainer — private credentials (SMTP, DB, secret token)
mathtrainer/private/env.php